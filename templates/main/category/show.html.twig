{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr id="categories">
                <th>Id</th>
                <th>Name</th>
            </tr>
            <tr>
                <td>{{ category.name }}</td>
                <td>{{ category.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">back to list</a>

    <a href="{{ path('category_edit', {'id': category.id}) }}">edit</a>

    {{ include('main/category/_delete_form.html.twig') }}
{% endblock %}
